---
name: <agent-name-kebab-case>
description: <когда использовать этого субагента — по этому описанию его выбирают. Будь конкретен: «Использовать при …».>
tools: Read, Edit, Grep, Glob, Bash
# tools опустить целиком, чтобы наследовать все инструменты родителя.
# model: sonnet   # опционально. Выбирай ПО СЛОЖНОСТИ задачи агента (экономия лимитов без потери
#                 # качества): read-only поиск/исследование/механическое извлечение → haiku/sonnet
#                 # (дешевле); сложное рассуждение/ревью/архитектура → opus. Опусти строку — наследует.
---

# <Название агента>

<Роль и экспертиза агента одним абзацем.>

## Контекст
- <ключевые модули/файлы, которые агент должен знать>

## Workflow
1. <что делает по шагам>

## Правила
- <границы, что НЕЛЬЗЯ делать>

<!--
Нативный субагент Claude Code: файл лежит в .claude/agents/, имя агента берётся из поля
`name`. Тело файла — системный промпт агента. Вызывается через Agent (subagent_type=name)
или автоматически по описанию. Подкаталоги в .claude/agents/ допустимы.
-->

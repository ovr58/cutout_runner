---
description: <одна строка: что делает команда — видна в списке /команд>
argument-hint: <подсказка по аргументам, напр. "<имя> | <опция>">
allowed-tools: Read, Edit, Glob
# model: claude-haiku-4-5-20251001   # опционально: переопределить модель для команды
---

# <Название команды>

<Что делает команда и в каком контексте вызывается.>

Вход: `$ARGUMENTS` — <описание>. (Отдельные позиционные: `$1`, `$2`, …)

## Шаги

1. …
2. …

<!--
Подсказки по нативному формату Claude Code:
- Имя команды = имя файла (этот файл → /_TEMPLATE.command; переименуй).
- Подкаталоги дают namespace: .claude/commands/plan/new.md → /plan:new.
- В теле можно подставлять вывод bash через строку, начинающуюся с ! (если allowed-tools
  разрешает Bash), и ссылаться на файлы через @path/to/file.
- allowed-tools ограничивает, чем команда может пользоваться. Опусти, чтобы наследовать всё.
-->

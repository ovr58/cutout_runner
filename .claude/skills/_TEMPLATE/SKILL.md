---
name: <skill-name-kebab-case>
description: Use when <конкретные триггеры/симптомы/типы запросов, при которых скил релевантен> — ТОЛЬКО условия срабатывания (не пересказ процесса), 3-е лицо, ≤500 символов. Весь фронтматтер ≤1024 символов.
# allowed-tools: Read, Grep, Glob   # опционально: ограничить инструменты скила
---

# <Название скила>

## Overview

<1–3 строки: что это за методика и какой результат она даёт. Почему она существует —
какую боль снимает.>

## When to Use (and When Not)

**Use when:**
- <триггер 1>
- <триггер 2>

**Don't use when:**
- <анти-кейс — когда скил НЕ нужен / есть путь лучше>

## Process / Quick Reference

1. …
2. …
3. …

<!-- Держи шаги нумерованными и сканируемыми. Если шаг длинный — вынеси в отдельный
файл рядом (script.sh, checklist.md) и сошлись на него; Claude подгрузит при активации. -->

## Common Mistakes

- ❌ <частая ошибка> → ✅ <как правильно>

## Cross-references

- REQUIRED BACKGROUND: <skill-name>   <!-- прочитать перед применением этого скила -->
- SUB-SKILL: <skill-name>             <!-- вызывается как шаг внутри -->

<!--
КОНВЕНЦИЯ ФОРМАТА (портирована из методики skills-системы, обезличена):

- Каталог .claude/skills/<name>/ с файлом SKILL.md (имя берётся из поля `name`).
- `description` — ГЛАВНЫЙ сигнал релевантности. Начинай с «Use when …» и перечисляй
  ТОЛЬКО условия, при которых скил нужен (симптомы, типы задач, ключевые слова).
  Не пересказывай в описании сам процесс — он в теле. 3-е лицо, ≤500 симв.
- Тело скила — EN, если это generic-методология кода (TDD, отладка, ревью);
  RU — только там, где это наша проектная конвенция (plan-lifecycle, мониторинг, память).
- Кросс-ссылки между скилами — строками `REQUIRED BACKGROUND: <name>` /
  `SUB-SKILL: <name>` БЕЗ `@`-синтаксиса.
- Параметризуй tech-specifics плейсхолдерами: `{{TEST_COMMAND}}`, `{{LINT_COMMAND}}`,
  `{{PRIMARY_LOG_PATH}}` и т.п. — чтобы скил переносился между проектами.
- Рядом можно класть вспомогательные файлы (скрипты, шаблоны, чеклисты).
- Активируется автоматически по релевантности либо как /<name>, если зарегистрирован.
- Все скилы перечислены одной строкой в `.claude/skills/INDEX.md`.
- Скилы, которые спавнят субагентов, ОБЯЗАНЫ ссылаться на cost-discipline
  (`docs/WORKING_RULES.md` §2). См. subagent-driven-development / dispatching-parallel-agents.
-->

# SPEC — техническая спецификация

Status: DRAFT (черновик интейка от <YYYY-MM-DD>) · Слой: **как это устроено**

> **Это черновик, собранный из интервью.** `> TODO(пользователь):` — места, где ответа не
> было; они **не додуманы** намеренно. Правь прямо здесь.
>
> **Куда что:** требования и «зачем» — в [`TZ.md`](TZ.md) · схемы и картинки — в
> [`VISUALS.md`](VISUALS.md) (ссылаться, не копировать) · термины — в
> [`../CONTEXT.md`](../CONTEXT.md) · сокращения — в [`GLOSSARY.md`](GLOSSARY.md) ·
> **необратимые решения — отдельными файлами в [`adr/`](adr/README.md)**, здесь только ссылка.
>
> **Правило меры:** для небольшого проекта нормальный размер этого файла — 1–3 страницы.
> Полнота ≠ объём. Если файл перерастает ~400 строк или появляется 3+ модуля с собственными
> контрактами — вынести `spec/<модуль>.md`, оставив здесь индекс и сквозные разделы.

---

## 1. Архитектура

**Общая идея решения** (2–4 строки: какой подход выбран и почему именно он):

<…>

- **Контекст системы** — схема [V-02](VISUALS.md#v-02)
- **Контейнеры** — схема [V-03](VISUALS.md#v-03)

**Точка входа:** `<путь к файлу/команде, с которой всё стартует>`

**Как запускается:**

```
<команда запуска>
```

**Как собирается:**

```
<команда сборки>
```

> Эти же команды попадают в плейсхолдеры запуска и тестов при инициализации шаблона
> (`INIT.md`, шаг «стековый профиль») — их использует скил `run`.

## 2. Стек и обоснование выбора

| Слой | Выбор | Почему именно он | Откатываемо? |
| --- | --- | --- | --- |
| Язык | <…> | <…> | дорого / легко |
| Фреймворк | <…> | <…> | |
| Хранилище | <…> | <…> | |
| Сборка/деплой | <…> | <…> | |

> Строка «дорого откатить» обязана иметь ADR в [`adr/`](adr/README.md). Если ADR нет —
> либо решение не такое уж необратимое, либо ADR забыли завести.

### Рассмотренные альтернативы

| Вариант | Чем был привлекателен | Почему отвергнут |
| --- | --- | --- |
| <…> | <…> | <…> |

> Раздел существует, чтобы отвергнутый вариант не переоткрывали каждой новой сессией.
> Пустой раздел — сигнал, что выбор не обсуждался, а не что альтернатив не было.

## 3. Модули и контракты

| Модуль | Ответственность | Что наружу (интерфейс) | Что внутри (скрыто) |
| --- | --- | --- | --- |
| <…> | <одна фраза> | <…> | <…> |

**Границы, которые нельзя пересекать:** <например: UI не ходит в БД напрямую; доступ к данным
только через репозитории>.

> Проектирование границ — скил `codebase-design` (глубокий модуль: узкий стабильный интерфейс,
> сложность спрятана). Эти же границы потом проверяет ревью веток.

## 4. Данные

- **Сущности и связи** — схема [V-04](VISUALS.md#v-04)
- **Хранилище:** <что и где>
- **Миграции:** <кто накатывает, как откатывается>
- **Персональные данные:** <есть/нет, перечень, где лежат> → требования в `TZ.md` NFR-05
- **Резервные копии / восстановление:** <…>

## 5. Интеграции

| Внешний сервис | Зачем | Протокол/контракт | Что делаем, если недоступен |
| --- | --- | --- | --- |
| <…> | <…> | <…> | <деградация, а не «падаем»> |

**Секреты и доступы:** где хранятся, как попадают в рантайм.
**Никогда — в коде и в git** (см. `.gitignore` и канон `AGENTS.md`).

## 6. Окружения и сборка

| Окружение | Назначение | Чем отличается от прода |
| --- | --- | --- |
| local | разработка | <…> |
| <staging> | <…> | <…> |
| prod | боевое | — |

**Конфигурация:** <где живёт, каков приоритет источников>

## 7. Тестирование

- **Что покрываем тестами обязательно:** <критичные пути>
- **Что не покрываем и почему:** <…>
- **Команда прогона:** `<…>`
- **Критерии приёмки** живут в `TZ.md` §9 в форме `Given/When/Then` — из них делаются тесты
  (скилы `test-driven-development`, `verification-before-completion`).

## 8. Релиз и эксплуатация

- **Как попадает к пользователю:** <деплой / сторы / инсталлятор / пакет>
- **Версионирование:** <…>
- **Наблюдаемость:** какие события видны в логах/метриках. Логирование — **только через
  логгер проекта**, без `print`/`console.log` в проде (канон).
- **Кто обслуживает после запуска:** <…>

## 9. Опыт и грабли

> Заполняется из блока **О** опросника (`INTAKE_QUESTIONNAIRE.md`) и разведки
> `../planning/reference/SPEC_INTAKE_RESEARCH.md` §3. **Каждый пункт — с причиной.**
> Запрет без причины через полгода читается как каприз и переоткрывается заново.

### Что уже работало — повторяем

| Решение | Где сработало | Почему повторяем |
| --- | --- | --- |
| <…> | <…> | <…> |

### Что НЕ сработало — не повторяем

| Решение / подход | Что пошло не так | Вывод |
| --- | --- | --- |
| <…> | <…> | <не используем X, потому что …> |

### Известные грабли стека

| Грабли | Когда всплывают | Что делаем заранее |
| --- | --- | --- |
| <из разведки по профилю стека> | <обычно поздно: на раздаче / на ревью стора / на нагрузке> | <…> |

> Если запрет жёсткий и дорогой к откату — он уезжает в [`adr/`](adr/README.md), а здесь
> остаётся ссылка.

## 10. Риски

| Риск | Влияние на архитектуру | Что делаем заранее |
| --- | --- | --- |
| <…> | <…> | <…> |

---

## 11. Handoff: как строить генеральный план

> **Этот раздел адресован модели, которая будет составлять
> [`../planning/reference/00_GENERAL_PLAN.md`](../planning/reference/00_GENERAL_PLAN.md).**
> Он существует, потому что генеральный план — самая дорогая к ошибке работа на проекте:
> ошибка в нём тиражируется во все последующие планы и коммиты.

### Протокол — исполнять по шагам

1. **Прочитать целиком:** [`TZ.md`](TZ.md) · этот файл · [`VISUALS.md`](VISUALS.md).
   Плюс канон [`../AGENTS.md`](../AGENTS.md) и карту скилов
   [`../.claude/skills/INDEX.md`](../.claude/skills/INDEX.md) (consult-first).
2. **До составления плана** — оценить работу по рубрике ниже и выдать пользователю
   рекомендацию **одной репликой**:

   > По этому ТЗ и спецификации: рекомендую **модель <X>, эффорт <Y>** — потому что
   > <1–2 строки обоснования по осям рубрики>. Переключись (`/model <X>`, при необходимости
   > смени эффорт) и напиши **«продолжить»**. Согласен с текущей моделью — тоже «продолжить».

3. **ОСТАНОВИТЬСЯ и ждать.** Не начинать генеральный план, пока пользователь не ответил.
   Смысл паузы в том, чтобы саму работу делала уже правильная модель, а не в том, чтобы
   отчитаться о рекомендации и продолжить на прежней.
4. **После «продолжить»** — объявить активную модель и вытекающую полосу доверия
   ([ADR-0002](adr/0002-trust-lane-by-model-not-host.md)), затем строить генеральный план по
   скилу `writing-plans`.

> Механика паузы — та же, что в [`../.claude/commands/review-branches.md`](../.claude/commands/review-branches.md)
> (шаг 1b). Вторую механику не изобретать.
>
> **В хостах без переключения модели** (или если пользователь работает на фиксированной
> модели) пауза не запрашивается — рекомендация остаётся рекомендацией, и это надо сказать
> вслух, а не молча продолжить.

### Рубрика: признак задачи → модель + эффорт

Оси оценки: **цена ошибки** · **объём кросс-файловых зависимостей** · **новизна домена** ·
**необратимость решений**.

| Признак работы | Модель | Эффорт |
| --- | --- | --- |
| Генеральный план с нуля: много модулей, необратимые решения (стек, границы, хранилище), цена ошибки высокая | **Claude Opus 5** (`claude-opus-5`) | `high` → `xhigh` |
| То же, но домен незнакомый / противоречивые требования / нужна длинная автономная проработка | **Claude Fable 5** (`claude-fable-5`) — самая способная широко доступная модель | `xhigh` → `max` |
| Один модуль, стек и границы уже зафиксированы, решения обратимые | **Claude Sonnet 5** (`claude-sonnet-5`) | `medium` → `high` |
| Механическая работа: переформатировать план, разнести пункты по файлам, свести таблицу | **Claude Haiku 4.5** (`claude-haiku-4-5`) | `low` |

Правила выбора:

- **По самому дорогому признаку, а не по среднему.** Один необратимый выбор внутри в целом
  простой задачи поднимает планку до Opus.
- **Эффорт по умолчанию — `high`.** `low`/`medium` — для рутины, `xhigh` — для сложной
  агентной/кодовой работы, `max` — когда корректность важнее стоимости.
- **Не понижать модель ради экономии** — это решение пользователя, не модели. Экономия
  делается эффортом и объёмом, а не подменой модели.
- Идентификаторы моделей проверять скилом `claude-api`, **не по памяти**: они меняются.
  Список выше сверен 2026-08-26.

### Честная оговорка

**Смена модели пользователем машинно не проверяется.** Ни хук, ни CI не знают, какая модель
активна: это держится на самообъявлении — ровно та же оговорка, что в `AGENTS.md` про полосы
доверия. Не выдавай паузу за гарантию: если пользователь написал «продолжить», не переключившись,
работа пойдёт на прежней модели, и заметить это можно только по объявлению модели в шаге 4.

### Что генеральный план обязан взять из этих документов

| Из документа | Во что превращается в `00_GENERAL_PLAN.md` |
| --- | --- |
| `TZ.md` §1 Цель, метрика успеха | «Цель проекта» |
| `SPEC.md` §1 Архитектура + [V-02](VISUALS.md#v-02)/[V-03](VISUALS.md#v-03) | «Архитектура (верхний уровень)» |
| `SPEC.md` §3 Модули | «Основные подсистемы» |
| `SPEC.md` §2 + `adr/` | «Принятые решения / ограничения» |
| `TZ.md` §3 Scope, §4 Сценарии | «Дорожная карта (вехи)» — по сценариям, не по слоям |
| `TZ.md` §11 Открытые вопросы | Первые записи в `../planning/BACKLOG.md`, а не выдуманные ответы |
| `SPEC.md` §9 Опыт и грабли | Ограничения на подходы — чтобы не пройти те же грабли заново |

---

## Связанные документы

| Документ | Что там |
| --- | --- |
| [`TZ.md`](TZ.md) | Требования — что и зачем |
| [`VISUALS.md`](VISUALS.md) | Все схемы и изображения — **единственный** визуальный артефакт |
| [`INTAKE_QUESTIONNAIRE.md`](INTAKE_QUESTIONNAIRE.md) | Вопросы, из которых собран этот документ |
| [`../planning/reference/SPEC_INTAKE_RESEARCH.md`](../planning/reference/SPEC_INTAKE_RESEARCH.md) | Откуда взята форма этих документов (стандарты, чужие реализации, грабли) |
| [`adr/`](adr/README.md) | Необратимые решения |
| [`../AGENTS.md`](../AGENTS.md) | Канон правил для всех AI-агентов |
